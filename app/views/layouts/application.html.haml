!!!
%html{:lang => "en"}
  %head
    %meta{:charset => "utf-8"}
      %meta{:content => "width=device-width, initial-scale=1.0", :name => "viewport"}
        %title= content_for?(:title) ? yield(:title) : "Oberlin ExCo"
        = csrf_meta_tags
        / Le HTML5 shim, for IE6-8 support of HTML elements
        /[if lt IE 9]
          <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
        = stylesheet_link_tag "application", :media => "all"
        /%link{:href => "/images/favicon.ico", :rel => "shortcut icon"}
        /%link{:href => "/images/apple-touch-icon.png", :rel => "apple-touch-icon"}
        /%link{:href => "/images/apple-touch-icon-72x72.png", :rel => "apple-touch-icon", :sizes=> "72x72"}
        /%link{:href => "/images/apple-touch-icon-114x114.png", :rel => "apple-touch-icon", :sizes=> "114x114"}
  %body
    .navbar.navbar-fixed-top
      .navbar-inner
        .container
          %a.btn.btn-navbar{"data-target" => ".nav-collapse", "data-toggle" => "collapse"}
            %span.icon-bar
            %span.icon-bar
            %span.icon-bar
            = link_to "Oberlin ExCo", root_path, :class => 'brand'
          .container.nav-collapse
            %ul.nav
              %li= link_to "Current ExCos", excos_path
              %li= link_to "All ExCos", all_excos_path
              -if can? :find, User
                %li= link_to "Users", users_path
              -if user_signed_in?
                %li= link_to "Edit account", edit_user_registration_path
                %li= link_to "Log out", destroy_user_session_path, :method => :delete
              / this is currently disabled because registration is not enabled ~ihm, 2012-08-16
              /-else
              /  %li= link_to "Log In", new_user_session_path
              /  %li= link_to "Register", new_user_registration_path
    / /.navbar-fixed-top
    .messages
      = render "layouts/messages"
    .container
      .content
        .row
          .span12
            = yield
        / /row
      / /content
      %footer
        %ul.nav
          -if user_signed_in?
            %li= link_to "Edit account", edit_user_registration_path
            %li= link_to "Log out", destroy_user_session_path, :method => :delete
          -else
            %li= link_to "Log In", new_user_session_path
        #credits
          %p © 2012, Oberlin Experimental College
          %p
            Designed and maintained by
            = link_to "Isaac Hollander McCreery", 'http://cs.oberlin.edu/~ihm'
            and
            Malcolm Balch-Crystal
    / /container
    / Javascripts
    / Placed at the end of the document so the pages load faster
    = javascript_include_tag "application"
