%h1 Listing excos
%table
  %tr
    %th Name
    %th Course Number
    %th Description
    %th Enrollment Limit
    %th Year
    %th Term
    %th
    -if can? :update, Exco
      %th
    -if can? :destroy, Exco
      %th
  - @excos.each do |exco|
    %tr
      %td= exco.name
      %td= exco.course_number
      %td= exco.description
      %td= exco.enrollment_limit
      %td= exco.year
      %td= exco.term
      %td= link_to 'Show', exco
      -if can? :update, Exco
        %td= link_to 'Edit', edit_exco_path(exco)
      -if can? :destroy, Exco
        %td= link_to 'Destroy', exco, confirm: 'Are you sure?', method: :delete
